// Keep deployment configuration in one place until the extension has a settings UI.
const APP_URL = 'https://solstime.day';
const target = `${APP_URL}/?surface=extension`;
window.location.replace(target);
