# Chrome toolbar extension

This Manifest V3 popup opens the canonical dial without replacing the browser's new page. It requests no host, tabs, storage, or browsing permissions.

## Load locally

1. Open `chrome://extensions`.
2. Enable Developer mode.
3. Choose **Load unpacked**.
4. Select this directory.

Before distribution, replace the production URL in `popup.html` and add final icon sizes.
